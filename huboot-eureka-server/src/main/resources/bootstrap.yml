spring:
  application:
    name: hu-eureka-server
  cloud:
    config:
      name: eureka
      profile: ${config.profile:local}
      label: newconfig
      uri: ${config.uri:http://}
  profiles:
    active: ${config.profile:local}